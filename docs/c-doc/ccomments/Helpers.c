/**
 * plank_helpers_truncate_middle:
 * @self: the (null) instance
 * @str: (in): &nbsp;.  <para>The string to truncate </para>
 * @max_length: (in): &nbsp;.  <para>Maximum length for the returned string </para>
 * 
 * Truncates a string at the middle to ensure it is no longer than max_length. If the string is truncated, an ellipsis will be inserted in the middle.
 * 
 * Returns: <para>A string not longer than max_length </para>
 */
/**
 * plank_helpers_visible_window_count:
 * @self: the (null) instance
 * @windows: &nbsp;
 */
