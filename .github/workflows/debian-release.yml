---
name: Build and Release - Debian Bookworm

on:
  release:
    types: [created]

jobs:
  build:
    name: Build on Debian Bookworm (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    container: debian:bookworm
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            runner: ubuntu-latest
          - arch: arm64
            runner: ubuntu-24.04-arm

    steps:
      - name: Checkout Code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Install Dependencies
        run: |
          apt-get update
          DEBIAN_FRONTEND=noninteractive apt-get install -y \
            git \
            dpkg \
            dpkg-dev \
            lintian \
            cmake \
            gettext \
            desktop-file-utils \
            libdbusmenu-glib-dev \
            libdbusmenu-gtk3-dev \
            meson \
            ninja-build \
            valac \
            libgnome-menu-3-dev \
            libxml2-utils \
            libgtk-3-dev \
            libgee-0.8-dev \
            libbamf3-dev \
            libwnck-3-dev \
            libcanberra-dev \
            libxi-dev \
            libxfixes-dev \
            bamfdaemon \
            build-essential \
            pkg-config

      - name: Build
        run: |
          meson setup --prefix=/usr build
          meson configure build -D enable-apport=false -D production-release=true
          meson compile -C build
          meson install --destdir="$(pwd)/release_build" -C build

      - name: Package
        run: |
          ARCH=$(dpkg --print-architecture)
          MULTIARCH=$(dpkg-architecture -qDEB_HOST_MULTIARCH)
          mkdir -p dist/plank-reloaded-debian
          cp -r release_build/* dist/plank-reloaded-debian/
          cp -r DEBIAN dist/plank-reloaded-debian/
          cd dist
          sed -i "s/^Architecture: .*/Architecture: ${ARCH}/" plank-reloaded-debian/DEBIAN/control
          VERSION=$(sed -n "s/^Version: //p" plank-reloaded-debian/DEBIAN/control)
          mv plank-reloaded-debian/DEBIAN/changelog plank-reloaded-debian/usr/share/doc/plank-reloaded/changelog.Debian
          mv plank-reloaded-debian/DEBIAN/copyright plank-reloaded-debian/usr/share/doc/plank-reloaded/
          gzip -9n plank-reloaded-debian/usr/share/doc/plank-reloaded/changelog.Debian
          gzip -9n plank-reloaded-debian/usr/share/man/man1/plank.1
          strip --strip-unneeded plank-reloaded-debian/usr/bin/plank
          strip --strip-unneeded "plank-reloaded-debian/usr/lib/${MULTIARCH}/libplank.so.1.0.0"
          find "plank-reloaded-debian/usr/lib/${MULTIARCH}/plank/docklets/" -type f -name "*.so" -exec strip --strip-unneeded {} \;
          dpkg-deb --build plank-reloaded-debian "plank-reloaded-debian_${VERSION}_${ARCH}.deb"

      - name: Run Lintian
        run: |
          cd dist
          lintian plank-reloaded-debian_*.deb || true

      - name: Verify Package
        run: |
          cd dist
          dpkg --info plank-reloaded-debian_*.deb
          dpkg --contents plank-reloaded-debian_*.deb

      - name: Upload Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: dist/plank-reloaded-debian_*.deb
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
